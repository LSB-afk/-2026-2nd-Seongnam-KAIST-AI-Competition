# Design

## Source of truth
- Status: Active. Refreshed: 2026-09-15.
- Surface: 독립 메뉴 `성남 3D 여행`, `?view=diorama`.
- Evidence: 사용자 55초 여행 영상의 29프레임 (`outputs/reference-video-2026-09-14/`), 기존 화면·팔레트, `docs/diorama.md`, 장소 자료 `src/lib/places.ts`.
- 최신 요청: 전체 화면 성남 지도에 인터넷에서 확인되는 관광명소를 넓게 반영하고, 실제 공개 지도 도로·건물·상점을 배치하며, 더 큰 사람과 많은 차량으로 활기를 만든다. 전체 화면과 지도 위 대시보드는 유지한다. 근거 화면은 `outputs/seongnam-immersive-development/user-current-1.png`, `user-current-2.png`에 보관한다.

## Brand
따뜻하고 조용한 도시 여행. 짙은 초록 글자, 크림색 건물, 녹지와 청록색 물. 공식 자료와 실제 사진을 신뢰 근거로 둔다. 번쩍이는 효과와 정밀 측량으로 오인할 표현을 피한다.

## Product goals
성남의 규모와 지역 간 관계를 첫 화면에서 느끼고 명소를 가까이 살펴본 뒤 같은 장소의 카드뉴스를 만든다. 첫 진입에서 세 구가 모두 보이고 장소 이동 시 같은 도시가 유지된다. 캔버스가 뷰포트 전체를 차지하고 패널을 접으면 조작 가능한 지도 공간이 넓어지는 것이 화면 성공 기준이다. 공개 지도에 등록된 도로·건물 외곽·상점을 실제 좌표로 반영한다. 미등록 시설의 전수 복원과 길 안내는 제공하지 않는다.

## Personas and jobs
지역을 배우는 청소년·시민이 데스크톱 또는 휴대폰에서 짧게 둘러보고 장소의 이야기를 발견한다.

## Information architecture
왼쪽 위 메뉴 버튼 → 기존 주 메뉴. 지도 위 지역 선택 → 성남 전체/수정·중원·분당 → 검색·지역별로 찾는 검증된 명소와 주변 상점 → 실제 사진/출처 → 카드 제작. 자동 감상은 명소 관찰 지점을 순회한다. 다른 작업실 화면은 기존 셸을 유지한다.

## Design principles
한 도시·한 캔버스. 랜드마크와 주변 도시를 함께 보여준다. 구 경계·도로 중심선·건물 외곽·상점 좌표는 공개 지도 원자료와 출처를 남긴다. 미등록 높이·도로 폭, 사람과 차량은 시각적 표현임을 짧게 안내한다. 전체 뷰에서는 지역·명소를, 거리 확대에서는 도로와 주변 가게를 읽게 한다.

## Visual language
기존 Noto Sans KR 및 초록/크림/금색 팔레트. 화면 전체 지도 위에 반투명 크림색 패널, 얇은 테두리와 약한 그림자를 사용한다. 제목·지역은 상단, 장소는 오른쪽, 감상은 하단에 배치해 가운데를 열어 둔다. 자동 감상 시작 없음. 사람·차량은 거리 위에서 움직이며 카메라와 함께 모션 줄이기·일시정지 설정을 존중한다. 사람 크기와 도시 활기는 사용자가 조절한다.

## Components
기존 Three renderer·투어 reducer·PlacePhoto·실제 지도·타미·카드 제작 연결 재사용. 명소 검색과 주변 상점 목록은 같은 정보 패널 안에서 제공한다. 도시 데이터의 기준일·건물/도로/상점 수와 범위를 확인할 수 있다. `diorama.css`는 전체 화면 지도·장소 패널·감상 설정을, `diorama-shell.css`는 3D 화면의 주 메뉴 팝오버와 셸을 소유한다. 패널 개폐는 UI 상태이며 도시 모형을 다시 만들지 않는다.

## Accessibility
명소/지역은 HTML 버튼으로도 선택 가능. 초점 테두리, 캔버스 키보드 회전/확대/Home, reduced-motion 유지. 이름표 충돌 시 일부 숨기되 HTML 목록은 유지한다. 패널은 aria-expanded와 controls를 연결하고 접힌 내용은 초점 순서에서 제외한다. Escape로 닫은 뒤 열기 버튼으로 초점을 돌린다. 지도와 패널이 함께 쓰이는 비모달 구조다.

## Responsive behavior
1440/1024px: 뷰포트 전체 지도와 접을 수 있는 오른쪽 패널. 390px: 전체 지도, 작은 상단 지역 선택, 접힌 상태로 시작하는 하단 명소 시트와 하단 재생 패널. 페이지는 스크롤하지 않고 정보 패널 안에서만 스크롤한다. 손가락 회전/핀치가 패널 밖에서 동작하고 카메라/재생/타미가 주요 버튼을 가리지 않는다.

## Interaction states
로딩은 도시 준비 상태. WebGL 실패 시 지역·장소 HTML 탐색과 실제 정보 유지. 빈 저장 필터는 재생 비활성화. 재진입/새로고침 자동 재생 없음. 빠른 선택은 마지막 의도가 이긴다.

## Content voice
짧고 친근한 한국어. '성남 전체', '지역 둘러보기', '명소 가까이 보기'. 실제 사실과 모형 표현을 구분하며 데이터에 포함된 건물·상점 수를 성남 전체의 현실 통계로 제시하지 않는다. 확인되지 않은 가게와 외관을 지어내지 않는다.

## Implementation constraints
기존 Next/React/Three 버전·의존성 유지. 인스턴싱·정적 그림자·DPR 1.5 상한·지연 로딩. 하나의 도시 자원 소유자 유지/해제. URL/저장/제작/기존 에이전트 동작 보존. 단위·실제 WebGL E2E·lint/typecheck/build와 3개 뷰포트 검수.

## Open questions
- 실제 Safari/저성능 휴대폰 성능은 물리 기기 확보 후 검증한다. 현재 자동 검증은 Chromium 데스크톱과 모바일 에뮬레이션이다.
