# Design

## Source of truth
- Status: Active. Refreshed: 2026-09-15.
- Surface: 독립 메뉴 `성남 3D 여행`, `?view=diorama`.
- Evidence: 사용자 55초 여행 영상의 29프레임 (`outputs/reference-video-2026-09-14/`), 기존 화면·팔레트, `docs/diorama.md`, 장소 자료 `src/lib/places.ts`.
- 최신 요청: 전체 화면 성남 지도에 인터넷에서 확인되는 관광명소를 넓게 반영하고, 실제 공개 지도 도로·건물·상점을 배치하며, 더 큰 사람과 많은 차량으로 활기를 만든다. 전체 화면과 지도 위 대시보드는 유지한다. 근거 화면은 `outputs/seongnam-immersive-development/user-current-1.png`, `user-current-2.png`에 보관한다.
- 조작 개선 요청: 지도를 클릭하고 끌어 원하는 위치로 자유롭게 이동한다. 기존 왼쪽 드래그/한 손가락 회전과 이름표 위 드래그 차단을 확인했다. 개선 전 화면과 재현은 `outputs/seongnam-map-navigation-development/`에 보관한다.

## Brand
따뜻하고 조용한 도시 여행. 짙은 초록 글자, 크림색 건물, 녹지와 청록색 물. 공식 자료와 실제 사진을 신뢰 근거로 둔다. 번쩍이는 효과와 정밀 측량으로 오인할 표현을 피한다.

## Product goals
성남의 규모와 지역 간 관계를 첫 화면에서 느끼고 명소를 가까이 살펴본 뒤 같은 장소의 카드뉴스를 만든다. 첫 진입에서 세 구가 모두 보이고 장소 이동 시 같은 도시가 유지된다. 캔버스가 뷰포트 전체를 차지하고 패널을 접으면 조작 가능한 지도 공간이 넓어지는 것이 화면 성공 기준이다. 공개 지도에 등록된 도로·건물 외곽·상점을 실제 좌표로 반영한다. 미등록 시설의 전수 복원과 길 안내는 제공하지 않는다.

## Personas and jobs
지역을 배우는 청소년·시민이 데스크톱 또는 휴대폰에서 짧게 둘러보고 장소의 이야기를 발견한다.

## Information architecture
왼쪽 위 메뉴 버튼 → 기존 주 메뉴. 지도 위 지역 선택 → 성남 전체/수정·중원·분당 → 검색·지역별로 찾는 검증된 명소와 주변 상점 → 실제 사진/출처 → 카드 제작. 자동 감상은 명소 관찰 지점을 순회한다. 다른 작업실 화면은 기존 셸을 유지한다.

## Design principles
한 도시·한 캔버스. 랜드마크와 주변 도시를 함께 보여준다. 구 경계·도로 중심선·건물 외곽·상점 좌표는 공개 지도 원자료와 출처를 남긴다. 미등록 높이·도로 폭, 사람과 차량은 시각적 표현임을 짧게 안내한다. 전체 뷰에서는 지역·명소를, 거리 확대에서는 도로와 주변 가게를 읽게 한다.

지도는 기본적으로 잡고 끌어 이동한다. 데스크톱 왼쪽 드래그와 모바일 한 손가락은 지면을 이동하고, 두 손가락은 확대와 이동을 함께 수행한다. 회전은 명시적인 모드 버튼이나 오른쪽 드래그로 선택한다. 이름표·명소 표지 이외의 바닥 클릭/탭은 현재 확대와 각도를 유지한 중심 이동이며 명소 선택을 바꾸지 않는다. 구 전체 받침면도 이동용 바닥으로 처리하고 구 선택은 이름표·상단 버튼으로 유지한다. 이름표도 끌기 시작점이 될 수 있고, 드래그·다중 터치·취소는 선택 클릭으로 처리하지 않는다. 휠은 커서 위치를 기준으로 확대한다. 사용자 입력은 자동 감상과 진행 중 카메라 이동을 즉시 중단하며, 놓은 뒤 관성으로 계속 흐르지 않는다.

## Visual language
기존 Noto Sans KR 및 초록/크림/금색 팔레트. 화면 전체 지도 위에 반투명 크림색 패널, 얇은 테두리와 약한 그림자를 사용한다. 제목·지역은 상단, 장소는 오른쪽, 감상은 하단에 배치해 가운데를 열어 둔다. 자동 감상 시작 없음. 사람·차량은 거리 위에서 움직이며 카메라와 함께 모션 줄이기·일시정지 설정을 존중한다. 사람 크기와 도시 활기는 사용자가 조절한다.

## Components
기존 Three renderer·투어 reducer·PlacePhoto·실제 지도·타미·카드 제작 연결 재사용. 명소 검색과 주변 상점 목록은 같은 정보 패널 안에서 제공한다. 도시 데이터의 기준일·건물/도로/상점 수와 범위를 확인할 수 있다. `diorama.css`는 전체 화면 지도·장소 패널·감상 설정을, `diorama-shell.css`는 3D 화면의 주 메뉴 팝오버와 셸을 소유한다. 패널 개폐는 UI 상태이며 도시 모형을 다시 만들지 않는다.

## Accessibility
명소/지역은 HTML 버튼으로도 선택 가능. 초점 테두리, 캔버스 방향키 이동(회전 모드에서는 회전), Shift+방향키 회전, +/− 확대, Home 시점 복원과 reduced-motion을 유지한다. 빈 곳 한 번 클릭으로도 이동하며 ‘위에서 보기’로 방향을 찾는다. 이동/회전 선택은 aria-pressed와 보이는 설명으로 알린다. 이름표 충돌 시 일부 숨기되 HTML 목록은 유지한다. 패널은 aria-expanded와 controls를 연결하고 접힌 내용은 초점 순서에서 제외한다. Escape로 닫은 뒤 열기 버튼으로 초점을 돌린다. 지도와 패널이 함께 쓰이는 비모달 구조다.

## Responsive behavior
1440/1024px: 뷰포트 전체 지도와 접을 수 있는 오른쪽 패널. 390px: 전체 지도, 작은 상단 지역 선택, 접힌 상태로 시작하는 하단 명소 시트와 하단 재생 패널. 페이지는 스크롤하지 않고 정보 패널 안에서만 스크롤한다. 한 손가락 이동/두 손가락 확대가 패널 밖에서 동작하고 카메라/재생/타미가 주요 버튼을 가리지 않는다. 이동·회전 모드와 카메라 버튼을 작은 도구 모음으로 묶어 화면을 좁게 만들지 않는다.

## Interaction states
로딩은 도시 준비 상태. WebGL 실패 시 지역·장소 HTML 탐색과 실제 정보 유지. 빈 저장 필터는 재생 비활성화. 재진입/새로고침 자동 재생 없음. 빠른 선택은 마지막 의도가 이긴다.

## Content voice
짧고 친근한 한국어. '성남 전체', '지역 둘러보기', '명소 가까이 보기'. 실제 사실과 모형 표현을 구분하며 데이터에 포함된 건물·상점 수를 성남 전체의 현실 통계로 제시하지 않는다. 확인되지 않은 가게와 외관을 지어내지 않는다.

## Implementation constraints
기존 Next/React/Three 버전·의존성 유지. 인스턴싱·정적 그림자·DPR 1.5 상한·지연 로딩. 하나의 도시 자원 소유자 유지/해제. URL/저장/제작/기존 에이전트 동작 보존. 단위·실제 WebGL E2E·lint/typecheck/build와 3개 뷰포트 검수.

## Open questions
- 실제 Safari/저성능 휴대폰 성능은 물리 기기 확보 후 검증한다. 현재 자동 검증은 Chromium 데스크톱과 모바일 에뮬레이션이다.

## City story experience — 2026-09-15
- 승인된 확장: 도시 이야기 만들기, 카드와 3D 장면 재생, 시간 렌즈, 타미 탐정단, 시민 기억 지도, 관광지 식별과 주변 먹거리. 기존 전체 화면 지도와 조작을 유지한다.
- 오른쪽 패널 안에 `둘러보기 / 내 이야기 / 시간·탐정 / 기억`을 둔다. 선택 장소에서 이야기에 담기, 실제 사진·공식 자료 선택, 메모·순서·구도 저장을 제공한다. 2~3곳을 한 이야기로 제작하며 사실 카드 3장과 마지막 상상 카드 1장이라는 기존 검수 계약을 유지한다. 모든 선택 장소를 결과에 포함한다.
- 메모는 작성 의도이며 공식 근거가 아니다. 카드의 장소는 서버가 지정하고 출처와 사진도 같은 장소에 속해야 한다. 이야기 연결선은 순서이며 도보 경로가 아니다.
- 3D 이야기 재생은 명시적으로 시작한다. 카드 이동과 저장 구도를 동기화하며 사용자 지도 조작·탭 비활성·모션 줄이기에서 자동 재생을 중단한다. 승인된 결과만 별도의 읽기 전용 공유 스냅샷으로 만든다. 공유 링크에는 메모나 초안이 들어가지 않는다. 로컬 서버 링크의 기기 접근 한계를 표시한다.
- `그때`는 날짜·출처가 있는 기록, `지금`은 현재 지도 데이터와 촬영일을 밝힌 실제 사진, `만약`은 AI 상상 제작이다. 오래된 사진을 현재 촬영 사진처럼 표시하지 않는다. 자료가 없으면 빈 상태와 출처 부족을 알린다.
- 탐정단은 공식 기록을 읽고 근거를 고르는 체험이다. 정답과 출처를 확인한 조각만 탐험 보고서에 넣는다. 기억은 개인 경험으로 표시하며 동의와 검토를 거쳐 이야기 메모로 활용한다. 초기 저장과 검토 범위는 이 기기임을 명시하고 공개 커뮤니티처럼 보이지 않게 한다.
- 관광지 표지는 실제 사진·장소 유형·이름을 함께 보여준다. 사진이 없는 장소의 외관을 추정하지 않는다. 먹거리는 유형별 색·아이콘으로 구분하고, 공식 선정/소개 출처와 날짜를 표시한다. 거리와 인기도를 혼동하지 않는다.
- 검증: 장소별 출처 격리, 저장 구도 검증, URL에 비공개 메모 미포함, 승인 전 공유 차단, 재생 중 수동 조작 우선, 기억 동의·검토·삭제, 탐정 정답 근거, 모바일 패널 접근성을 단위 및 실제 브라우저에서 확인한다.
